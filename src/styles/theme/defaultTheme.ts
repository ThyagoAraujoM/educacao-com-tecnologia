import { createTheme } from "@mui/material";
export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#e74040",
    },
    secondary: { main: "#252b42", light: "#737373" },
  },
});
